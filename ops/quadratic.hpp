#include <amath-ng.hpp>

amath_float getdiscrim(amath_float a, amath_float b, amath_float c); //Gets the discriminant given a, b, and c
vector<amath_float> solve(amath_float a, amath_float b, amath_float discrim); //Solves the equation given a, b, and the discriminant and returns the solutions in an array
